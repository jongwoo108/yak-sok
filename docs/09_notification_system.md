# 09. 사용자 연결 및 알림 시스템

> 작성일: 2026-01-17

## 개요

시니어와 보호자 간 연결 및 푸시 알림 시스템입니다. 초대 코드를 통해 사용자를 연결하고, 연결된 사용자 간에 직접 알림을 주고받을 수 있습니다.

---

## 1. 초대 코드를 통한 사용자 연결

### 1.1 기능 설명

앱 내에서 시니어와 보호자를 연결하는 기능입니다. 6자리 숫자 초대 코드를 통해 연결합니다.

| 단계 | 행동 | 설명 |
|------|------|------|
| 1 | 코드 생성 | 시니어 또는 보호자가 "연결 추가" → "코드 생성" |
| 2 | 코드 공유 | 카카오톡, 문자 등으로 6자리 코드 전달 |
| 3 | 코드 입력 | 상대방이 "연결 추가" → 코드 입력 → 연결 완료 |

### 1.2 데이터 모델

```python
# backend/apps/users/models.py

class InviteCode(models.Model):
    """사용자 연결 초대 코드"""
    
    user = models.ForeignKey(User, on_delete=models.CASCADE, related_name='invite_codes')
    code = models.CharField(max_length=6, unique=True)  # 6자리 숫자
    is_used = models.BooleanField(default=False)
    used_by = models.ForeignKey(User, null=True, blank=True, on_delete=models.SET_NULL)
    expires_at = models.DateTimeField()  # 24시간 후 만료
    created_at = models.DateTimeField(auto_now_add=True)
    
    @property
    def is_expired(self):
        return timezone.now() > self.expires_at
    
    @property
    def is_valid(self):
        return not self.is_used and not self.is_expired
```

### 1.3 API 엔드포인트

#### 내 초대 코드 조회

```
GET /api/users/invite/
```

**응답:**
```json
{
    "code": "123456",
    "expires_at": "2026-01-18T15:30:00Z"
}
```

#### 초대 코드 생성

```
POST /api/users/invite/
```

**응답:**
```json
{
    "success": true,
    "invite": {
        "code": "123456",
        "expires_at": "2026-01-18T15:30:00Z"
    },
    "message": "초대 코드가 생성되었습니다. 24시간 내에 사용해주세요."
}
```

#### 초대 코드로 연결

```
POST /api/users/invite/accept/
```

**요청:**
```json
{
    "code": "123456"
}
```

**응답 (성공):**
```json
{
    "success": true,
    "message": "홍길동님과 연결되었습니다.",
    "relation": { ... }
}
```

**에러 응답:**
```json
{ "error": "유효하지 않은 초대 코드입니다." }
{ "error": "이미 사용된 초대 코드입니다." }
{ "error": "만료된 초대 코드입니다." }
{ "error": "시니어와 보호자만 연결할 수 있습니다." }
{ "error": "이미 연결된 사용자입니다." }
```

### 1.4 연결 규칙

- **시니어 ↔ 보호자**만 연결 가능 (같은 역할끼리 연결 불가)
- 한 시니어에 여러 보호자 연결 가능
- 한 보호자가 여러 시니어 관리 가능
- 초대 코드는 **24시간 후 만료**
- 새 코드 생성 시 기존 미사용 코드 자동 만료

### 1.5 화면 구성 (설정 화면)

```
┌─────────────────────────────────────┐
│  연결 관리                [+ 연결 추가] │
│  ────────────────────────────────   │
│  [김] 김보호자               [✕]   │  ← 탭: 알림 전송 / ✕: 연결 해제
│       탭하여 알림 보내기           │
│  ────────────────────────────────   │
│  연결된 사용자가 없습니다          │  ← 연결이 없는 경우
│  보호자와 연결하여 복약 관리를 받으세요 │
└─────────────────────────────────────┘
              ↓ [+ 연결 추가] 탭
┌─────────────────────────────────────┐
│           사용자 연결               │
│  ────────────────────────────────   │
│  내 초대 코드                       │
│  상대방에게 이 코드를 전달하세요    │
│  ┌─────────────────────────────┐   │
│  │     1 2 3 4 5 6        [📤] │   │  ← 공유 버튼
│  └─────────────────────────────┘   │
│             — 또는 —               │
│  초대 코드 입력                     │
│  상대방에게 받은 6자리 코드 입력    │
│  ┌─────────────────────┐ ┌─────┐   │
│  │      000000         │ │연결 │   │
│  └─────────────────────┘ └─────┘   │
│              [닫기]                 │
└─────────────────────────────────────┘
```

---

## 2. 사용자 간 알림 전송

### 2.1 기능 설명

연결된 사용자(시니어-보호자) 간에 직접 알림을 보낼 수 있습니다.

| 발신자 | 수신자 | 메시지 유형 |
|--------|--------|-------------|
| 보호자 | 시니어 | 안부 확인, 약 드셨나요? |
| 시니어 | 보호자 | 괜찮아요, 도움 필요해요 |

### 2.2 API 엔드포인트

#### 알림 전송

```
POST /api/alerts/send/
```

**요청:**
```json
{
    "recipient_id": 1,
    "message_type": "check_in",
    "custom_message": ""
}
```

**메시지 유형:**
- `check_in`: 안부 확인
- `reminder`: 약 드셨나요?
- `im_ok`: 괜찮아요
- `need_help`: 도움 필요해요
- `custom`: 직접 입력

**응답:**
```json
{
    "success": true,
    "message": "홍길동님에게 알림을 보냈습니다.",
    "alert_id": 123
}
```

### 2.3 권한 검증

- `GuardianRelation` 테이블에서 연결 관계 확인
- 연결되지 않은 사용자에게는 알림 전송 불가 (403 Forbidden)

### 2.4 화면 구성 (프로필 화면)

```
┌─────────────────────────────────────┐
│  연결된 보호자                      │
│  ────────────────────────────       │
│  [김] 김보호자                      │
│       탭하여 알림 보내기     [📤]   │
└─────────────────────────────────────┘
          ↓ 탭 시 모달 열림
┌─────────────────────────────────────┐
│         알림 보내기                 │
│       김보호자님에게                │
│  ┌─────────┐  ┌─────────┐          │
│  │   ✓     │  │   !     │          │
│  │괜찮아요 │  │도움필요 │          │
│  └─────────┘  └─────────┘          │
│              [닫기]                 │
└─────────────────────────────────────┘
```

---

## 3. 푸시 알림 시스템

### 3.1 아키텍처

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Mobile    │───▶│   Django    │───▶│   Celery    │
│  (Expo Go)  │    │   Backend   │    │   Worker    │
└─────────────┘    └─────────────┘    └──────┬──────┘
                                             │
                                             ▼
                                      ┌─────────────┐
                                      │  Expo Push  │
                                      │   Service   │
                                      └──────┬──────┘
                                             │
                                             ▼
                                      ┌─────────────┐
                                      │   Mobile    │
                                      │   Device    │
                                      └─────────────┘
```

### 3.2 푸시 알림 플로우

1. **사용자 알림 전송 요청** → `POST /api/alerts/send/`
2. **Django** → Celery 태스크 큐에 추가 (`send_push_notification.delay()`)
3. **Celery Worker** → FCM Service 호출
4. **FCM Service** → Expo Push API 직접 호출 (urllib 사용)
5. **Expo Push Service** → 대상 기기에 알림 전달

### 3.3 FCM Service 구현

```python
# apps/alerts/fcm_service.py

class FCMService:
    @classmethod
    def send_notification(cls, token, title, body, data=None):
        # Expo Push Token 처리
        if token.startswith('ExponentPushToken'):
            # urllib로 Expo Push API 직접 호출
            url = "https://exp.host/--/api/v2/push/send"
            payload = {
                "to": token,
                "title": title,
                "body": body,
                "data": data or {},
                "sound": "default",
            }
            # SSL 검증 비활성화 (개발 환경)
            ssl_context = ssl.create_default_context()
            ssl_context.check_hostname = False
            ssl_context.verify_mode = ssl.CERT_NONE
            ...
        
        # Native FCM Token 처리
        else:
            # Firebase Admin SDK 사용
            ...
```

### 3.4 Windows 환경 SSL 문제 해결

Windows에서 `requests` 라이브러리 사용 시 SSL 권한 에러 발생:

```
SSLError: PermissionError(13, 'Permission denied')
```

**해결책**: `urllib`를 사용하여 SSL 검증 비활성화

```python
import urllib.request
import ssl

ssl_context = ssl.create_default_context()
ssl_context.check_hostname = False
ssl_context.verify_mode = ssl.CERT_NONE

with urllib.request.urlopen(req, context=ssl_context) as response:
    result = json.loads(response.read().decode('utf-8'))
```

> **주의**: 이 방법은 개발 환경 전용입니다. 프로덕션에서는 정상적인 SSL 인증서 검증을 사용해야 합니다.

### 3.5 Celery 설정

#### Windows 실행 명령

```bash
cd backend
celery -A core worker -l info --pool=solo
```

**주의**: Windows에서는 `--pool=solo` 필수 (기본 prefork pool 미지원)

#### 개발 환경 설정 (settings.py)

```python
# Celery 없이 동기 실행 (개발용)
CELERY_TASK_ALWAYS_EAGER = DEBUG  # DEBUG=True일 때만
```

---

## 4. 관련 파일 목록

### Backend

| 파일 | 설명 |
|------|------|
| `apps/alerts/fcm_service.py` | Expo/FCM 푸시 알림 발송 |
| `apps/alerts/tasks.py` | Celery 비동기 태스크 |
| `apps/alerts/views.py` | `/alerts/send/` 엔드포인트 |
| `apps/alerts/serializers.py` | `SendAlertSerializer` |
| `apps/users/models.py` | `InviteCode` 모델 |
| `apps/users/views.py` | `InviteCodeView`, `AcceptInviteView` |
| `apps/users/serializers.py` | `InviteCodeSerializer`, `AcceptInviteSerializer` |
| `apps/users/urls.py` | `/invite/`, `/invite/accept/` 엔드포인트 |
| `core/settings.py` | Celery 설정 |

### Mobile (React Native)

| 파일 | 설명 |
|------|------|
| `app/(tabs)/profile.tsx` | 알림 전송 + 연결 관리 UI |
| `services/api.ts` | 알림 전송 API, 초대 코드 API |

---

## 5. 마이그레이션

```bash
cd backend
python manage.py makemigrations users --name add_invite_code
python manage.py migrate
```

마이그레이션 파일:
- `users/0004_add_invite_code.py`

---

## 6. 개발 환경 실행

### 전체 서비스 실행 순서

```bash
# 1. Redis 실행 (Docker)
docker run -d -p 6379:6379 redis

# 2. Django 서버
cd backend
python manage.py runserver 0.0.0.0:8000

# 3. Celery Worker (새 터미널)
cd backend
celery -A core worker -l info --pool=solo

# 4. Mobile App (새 터미널)
cd mobile
npx expo start
```

### 테스트 순서

1. **회원가입/로그인** (시니어 1명, 보호자 1명)
2. **사용자 연결** (시니어가 코드 생성 → 보호자가 코드 입력)
3. **알림 전송** (보호자 → 시니어 알림 전송)
4. **알림 수신** 확인 (시니어 기기에서 푸시 알림 확인)
