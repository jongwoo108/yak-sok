# iOS 기능 오류 해결 및 SDK 53 업그레이드 리포트

## 1. 개요
Expo SDK 51로 다운그레이드한 이후 발생한 iOS 앱의 주요 기능(카메라, 갤러리, 로그아웃, 약 등록, AI 분석) 오류를 디버깅하고, 보다 안정적인 환경을 위해 SDK 53으로 업그레이드하는 과정입니다.

## 2. 발견된 문제 및 해결 내용

### 2.1 카메라 및 갤러리 실행 불가
- **현상**: 권한 요청 팝업은 뜨지만 허용 후 앱이 멈추거나 실행되지 않음.
- **원인**: SDK 51의 `expo-image-picker`에서 `mediaTypes: ['images']` (배열 형태) 문법이 제대로 처리되지 않음.
- **해결**: SDK 51에서는 `ImagePicker.MediaTypeOptions.Images` (Enum)를 사용하여 해결. (SDK 53/54에서는 다시 배열 형태로 원복 및 타입 단언 적용)

### 2.2 로그아웃 후 흰 화면 현상
- **현상**: 로그아웃 버튼 클릭 시 화면이 하얗게 멈추고 로그인 페이지로 이동하지 않음.
- **원인**: `_layout.tsx`의 Stack Navigator에 인증 그룹(`(auth)`) 외부의 개별 라우트들이 명시적으로 등록되지 않아 SDK 51/52 환경에서 라우팅 엔진이 경로를 찾지 못함.
- **해결**: `medications/scan`, `profile/edit` 등 주요 경로를 `_layout.tsx`의 Stack에 명시적으로 추가.

### 2.3 API 인증 에러 ("자격인증 데이터가 제공되지 않았습니다")
- **현상**: 메인 화면은 진입했으나 약 등록이나 AI 분석 시 인증 실패 메시지 발생.
- **원인**: SDK 51의 `expo-secure-store`와 Axios 인터셉터 간의 타이밍 문제 혹은 토큰 유지 문제로 추정. 
- **조치**: SDK 53 업그레이드를 통해 네이티브 모듈의 안정성 확보 시도 중.

## 3. Expo SDK 53 업그레이드

- **결정 배경**: SDK 51의 여러 불안정한 요소(인증, 라우팅)를 해결하기 위해 React 19와 React Native 0.79를 사용하는 SDK 53으로 업그레이드 결정.
- **주요 설정**:
    - `newArchEnabled: false`: 여전히 TurboModules 관련 크래시 위험이 있으므로 New Architecture는 비활성화 유지.
    - `react-native-safe-area-context`: 버전 충돌 방지를 위해 `overrides` 설정.

## 4. EAS 빌드 한도 우회

- **문제**: 기존 계정(`jongwoo108`)의 이번 달 iOS 빌드 한도(15회) 초과.
- **해결**: 
    1. ✅ 새 Expo 계정(`jongwoo3353`) 생성.
    2. ✅ `app.config.js`의 `owner` 필드를 `jongwoo3353`으로 변경.
    3. ✅ `eas init`으로 새 계정에 프로젝트 연결 (`projectId: 7a4687ce-2d65-457c-913b-6e6af74d717f`).
    4. ✅ `app.config.js`에 `extra.eas.projectId` 설정 완료.

## 5. SDK 53 업그레이드 후 발생한 문제 (2026-01-21)

### 5.1 iOS 크래시 재발
- **증상**: SDK 53으로 업그레이드 후 iOS 앱 실행 즉시 크래시
- **크래시 로그 분석**: 
  - `com.facebook.react.ExceptionsManagerQueue`에서 abort 발생
  - New Architecture OFF 상태임에도 크래시 발생
  - **결론**: JS 영역에서 치명적인 에러 발생 가능성 높음
- **시도한 해결책**:
  - ✅ `expo.newArchEnabled: false` 명시적 설정
  - ✅ `expo-build-properties` 플러그인으로 New Architecture 비활성화
  - 🔄 Development 빌드로 JS 에러 확인 시도 중

### 5.2 EAS 빌드 타겟 이름 오류
- **증상**: `Could not find target 'app' in project.pbxproj`
- **원인**: Fastlane이 "app" 타겟을 찾지만, Expo가 생성한 타겟 이름이 다름
- **시도한 해결책**:
  - ❌ `slug` 변경 시도 → EAS 프로젝트 slug 불일치 에러
  - 🔄 `expo.name`을 "약속" → "YakSok"으로 변경 (영어로 변경하여 타겟 이름 문제 해결 시도)
- **상태**: 아직 빌드 미실행, 타겟 이름 문제 해결 대기 중

### 5.3 Dev Client 연결 문제
- **증상**: Dev Client 설치 후 딥링크로 연결 시 앱이 즉시 꺼지고 PC 메트로 로그가 전혀 찍히지 않음
- **원인 추정**: 네이티브 레벨 크래시 또는 번들 받기 전 단계에서 실패
- **상태**: 빌드 타겟 이름 문제 해결 후 재시도 예정

## 6. 향후 작업 계획

### 우선순위 1: 빌드 타겟 이름 문제 해결
1. `eas build --platform ios --profile development` 재실행
2. "Configure Xcode project" 로그에서 실제 타겟 이름 확인
3. 타겟 이름에 맞춰 `ios/Gymfile` 생성 또는 `expo prebuild` 실행

### 우선순위 2: JS 에러 원인 파악
1. Development 빌드 성공 후 Dev Client 설치
2. Dev Client로 연결하여 PC 메트로 로그에서 정확한 JS 에러 확인
3. 에러 메시지 기반으로 코드 수정

### 우선순위 3: 대안 검토
- SDK 51로 롤백 (이전에 작동했던 버전)
- 또는 SDK 52로 시도 (중간 단계)

---

> **최종 업데이트**: 2026-01-21  
> **상세 트러블슈팅 문서**: [13_ios_sdk53_troubleshooting.md](./13_ios_sdk53_troubleshooting.md)
